# 本地开发环境变量示例文件
# 复制此文件为 .env 并修改对应的值（.env 文件不会被提交到Git）

# AES加密密钥（必须16字节）
# 生产环境请使用强随机密钥，本地开发可以使用简单值
AES_SECRET_KEY=local_dev_key123

# 使用说明：
# 1. 复制此文件为 .env
# 2. 修改 AES_SECRET_KEY 的值
# 3. 在 IDEA 中配置环境变量：
#    Run -> Edit Configurations -> Environment Variables -> 添加 AES_SECRET_KEY
# 4. 或者在 PowerShell 中设置：
#    $env:AES_SECRET_KEY="local_dev_key123"
